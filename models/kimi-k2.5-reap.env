# =============================================================================
# Kimi-K2.5-PRISM-REAP-530B-A32B  --  Ex0bit/Kimi-K2.5-PRISM-REAP-530B-A32B
# =============================================================================
# FP8 model. Requires CUDA >= 12.8 for flashinfer block scaling.
# Will fail on nodes without sufficient CUDA.

MODEL_NAME="Ex0bit/Kimi-K2.5-PRISM-REAP-530B-A32B"

VLLM_ARGS="--tensor-parallel-size 4 \
    --served-model-name kimi-k2.5-reap \
    --enable-auto-tool-choice \
    --tool-call-parser kimi_k2 \
    --reasoning-parser kimi_k2 \
    --mm-encoder-tp-mode data \
    --enable-expert-parallel \
    --trust-remote-code \
    --enable-prefix-caching \
    --override-generation-config.repetition_penalty 1.07 \
    --load-format fastsafetensors"
