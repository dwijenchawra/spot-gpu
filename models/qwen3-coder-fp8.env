# =============================================================================
# Qwen3-Coder-Next-FP8  --  Qwen/Qwen3-Coder-Next-FP8
# =============================================================================
# FP8 coding model. Requires CUDA >= 12.8 for flashinfer block scaling.
# Environment: CUDA 13 venv

VENV_PROFILE="cu13/.venv"
REQUIRES_CUDA13="true"

MODEL_NAME="Qwen/Qwen3-Coder-Next-FP8"

VLLM_ARGS="--tensor-parallel-size 4 \
    --max-model-len 262144 \
    --enable-expert-parallel \
    --tool-call-parser qwen3_xml \
    --enable-auto-tool-choice \
    --served-model-name qwen3_coder \
    --enable-prefix-caching \
    --load-format fastsafetensors"
